{
  "name": "day-4-lap",
  "version": "1.0.0",
  "description": "A simple project to serve a JSON API with local images using json-server.",
  "main": "index.js",
  "scripts": {
    "start": "json-server data.json -p 3009 -w --static ./public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "json-server",
    "API",
    "local-images"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
